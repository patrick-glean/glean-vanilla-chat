{
  "name": "glean-vanilla-chat",
  "version": "1.0.0",
  "description": "A vanilla JavaScript chat application",
  "type": "module",
  "scripts": {
    "build": "rm -rf -f dist/* && tsc && cp src/styles.css dist/ && cp index.html dist/ && node scripts/screenshot.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'npm run build' & http-server dist --cors='*' --proxy https://support-lab-be.glean.com"
  },
  "dependencies": {
    "puppeteer": "^21.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "typescript": "^5.0.0",
    "http-server": "^14.0.0"
  }
} 